node_modules
.vercel

# Build — nunca versionar: o bundle carrega as variáveis de ambiente embutidas
dist
*.local

# Segredos
.env
.env*
.env*.local
!.env.example

# Editores / SO
.DS_Store
*.log

# Chave de servico do Firebase (segredo — nunca versionar)
scripts/serviceAccount.json

# Backups do RTDB (contêm dado real de aluno — nunca versionar)
backups/

# Cache local do Firebase CLI / emulador (scripts/test-firestore-rules.mjs)
.firebase/
firestore-debug.log
firebase-debug.log
ui-debug.log
